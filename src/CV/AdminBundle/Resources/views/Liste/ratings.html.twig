{% extends "CVAdminBundle::layout.html.twig" %}

{% block body %}
    {% stylesheets '@CVAdminBundle/Resources/public/css/listes.css' %}
        <link rel="stylesheet" href="{{ asset_url }}" type="text/css" />
    {% endstylesheets %}
    <div id="page-wrapper">
        <div class="container" style="margin-left:0px;">
            <div class="row">
                <h1 class="page-header">Avis</h1>
            </div> 
            <div class="row">
               <div class="container">
                    <div class="row">
                        <div class="panel panel-default widget">
                            <div class="panel-heading">
                                <span class="glyphicon glyphicon-comment"></span>
                                <h3 class="panel-title">Du plus récent au plus ancien</h3>
                                <span class="label label-info">{{ listRatings | length }}</span>
                            </div>
                            <div class="panel-body">
                                <ul class="list-group">
                                    {% for rating in listRatings %}
                                        {% include 'CVAdminBundle:Liste:rating.html.twig' with {'rating': rating} %}
                                    {% endfor %}
                                </ul>
                                <a href="#" class="btn btn-primary btn-sm btn-block" role="button"><span class="glyphicon glyphicon-refresh"></span> More</a>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
{% block javascripts %}
    <script src="//ajax.googleapis.com/ajax/libs/jquery/1.11.1/jquery.min.js"></script>
    <script src="//maxcdn.bootstrapcdn.com/bootstrap/3.2.0/js/bootstrap.min.js"></script>
    {% javascripts 
        '@CVAdminBundle/Resources/public/js/metisMenu.min.js'
        '@CVAdminBundle/Resources/public/js/admin.js' %}
        <script type="text/javascript" src="{{ asset_url }}"></script>
    {% endjavascripts %}
{% endblock %}