<tr id="notif_{{notification.id}}">
	<td class="col-md-8">{{ notification.reservation.ride.departureDate }}  : Le voyage {{ notification.reservation.ride.departure }} -> {{ notification.reservation.ride.arrival }} a été effectué. </br>
		Vous pouvez laisser un avis 
			{% if notification.reservation.ride.user == app.security.getToken().getUser() %}
				au passager
			{% else %}
				au conducteur 
			{% endif %}
			{{ notification.reservation.ride.user }}
	</td>
	<td><a href="{{ path('cv_platform_leave_rating', {'id': notification.id }) }}" class="btn btn-primary"><i class="glyphicon glyphicon-plus"></i> Laisser un avis</a></td>
	<td><a id="{{notification.id}}" class="delete"><img src="{{ asset('icon/delete.png') }}"></img></a></td>
</tr> 