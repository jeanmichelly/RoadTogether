{% extends "CVPlatformBundle::layout.html.twig" %}

{% block title %}
    Mes réservations passées - {{ parent() }}
{% endblock %}

{% block menu_lateral %}  
    {{ include("CVPlatformBundle:Reservation:menu.html.twig") }}  
{% endblock %}

{% block cvplatform_body %}
    <h2>Liste de mes réservations passées</h2>

{% if listPastReservations|length == 0 %}
        <p>
        {% for message in app.session.flashbag.get('info') %}

        <p class="alert alert-info">{{ message }}</p> 
        {% endfor %}
    </p>

    {% else %}


    {% for pastReservation in listPastReservations %}
        <div class="well">
            <i>Annonce créé le {{ pastReservation.ride.offerPublished|date('d/m/Y à H:i:s') }}</>
            <li>Lieu de départ : {{ pastReservation.ride.departure }}</li>
            <li>Lieu d'arrivé : {{ pastReservation.ride.arrival }}</li>
            <li>Prix : {{ pastReservation.ride.price }}</li>
            <p>
                <a href="{{ path('cv_platform_view', {'id': pastReservation.ride.id   }) }}" class="btn btn-default"><i class="glyphicon glyphicon-eye-open"></i>Voir détails</a>
            </p>
        </div>
    {% endfor %}

    <ul class="pagination">
        {% for p in range(1, nbPages) %}
            <li{% if p == page %} class="active"{% endif %}><a href="{{ path('cv_platform_past_reservations', {'page': p}) }}">{{ p }}</a></li>
        {% endfor %}
    </ul>
  {% endif %} 
{% endblock %}
